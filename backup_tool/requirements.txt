questionary>=2.0,<3.0
rich>=15.0,<16.0
bcrypt>=5.0,<6.0

# Platform-specific (optional — import via try/except):
# macOS: pyobjc-framework-DiskArbitration  → native USB mount events
# Linux: pyudev                          → native USB events
# Windows: pywin32                       → alternative to ctypes WinAPI